info:
  name: Create todo
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/todo"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "title": "new todo",
        "content": "123123213",
        "isCompleted": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
