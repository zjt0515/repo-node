info:
  name: Get todo by id
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/todo/:id"
  params:
    - name: id
      value: "0"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
