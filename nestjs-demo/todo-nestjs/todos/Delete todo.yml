info:
  name: Delete todo
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/todo/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
