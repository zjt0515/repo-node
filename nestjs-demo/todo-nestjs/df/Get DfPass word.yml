info:
  name: Get DfPass word
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/df/password"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
