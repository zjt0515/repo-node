info:
  name: Update todo
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{BASE_URL}}/todo/:id"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "title": "thinking"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
